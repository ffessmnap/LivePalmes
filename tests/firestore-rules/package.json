{
  "name": "livepalmes-firestore-rules-tests",
  "private": true,
  "scripts": {
    "test": "cd ../.. && firebase emulators:exec --project demo-livepalmes --config firebase.json --only firestore \"node --test tests/firestore-rules/firestore-rules.test.cjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.10.0",
    "firebase-tools": "^15.0.0"
  }
}
